# Блоки кода и цитаты

## Inline-код

Команда `git status` показывает состояние репозитория.

Также можно использовать `git add .` для добавления всех файлов.

## Блок кода с указанием языка (Python)

```python
print("Hello!")
name = input("Введите имя: ")
print(f"Привет, {name}!")
```

## Блок кода на C#

```csharp
Console.WriteLine("Введите первое число: ");
double number1 = Convert.ToDouble(Console.ReadLine());
double sum = number1 + number2;
Console.WriteLine($"Сумма: {sum}");
```

## Блок кода без указания языка

```
Просто блок кода без языка
string name;
name = Console.ReadLine();
Console.WriteLine(name)
```

## Цитаты

> Это цитата в Markdown.

### Вложенная цитата

> Первый уровень
>> Второй уровень
